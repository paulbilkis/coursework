#include <stdio.h>
#include <stdlib.h>
typedef struct str{
  char data[TITLE_SIZE];
  struct str *n=NULL;
  struct str *prev=NULL;
}str;

typedef struct source{
  unsigned id;
  int num;
  str *title;
  struct source *n=NULL;
  struct source *prev=NULL;
}source;

typedef struct a_src{
  source *src;
  unsigned num;
  struct a_src n=NULL;
  struct a_src prev=NULL;
}a_src;

typedef struct receipt{
  unsigned id;
  
}receipt;
